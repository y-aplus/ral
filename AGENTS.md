# Agent guidance

When you make a reusable modification, consider offering to run `ral publish`.

Publishing is optional. It sends source code to a public GitHub repository and must not be done without the user's explicit approval. Explain what will be published and let the user review the changed files before invoking `ral publish --yes`.
